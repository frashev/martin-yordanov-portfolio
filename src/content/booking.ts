import type { BookingEmbed } from "./types";

export const bookingEmbed: BookingEmbed = {
  title: "Project conversation",
  summary:
    "A booking or calendar embed can go here after Martin chooses a scheduling provider. For now, the contact form is the primary action.",
};
